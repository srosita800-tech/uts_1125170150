name: uts_1125170150
description: "Flutter Firebase Authentication App"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase Core - WAJIB ada
  firebase_core: ^3.0.0

  # Firebase Authentication
  firebase_auth: ^5.0.0

  # Untuk navigasi yang lebih baik
  go_router: ^14.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true